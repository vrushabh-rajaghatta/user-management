<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <InternalsVisibleTo Include="Ligature.CatalogueSync.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Platform\Ligature.Platform.Persistence\Ligature.Platform.Persistence.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Ligature.CatalogueSync</RootNamespace>
    <AssemblyName>Ligature.CatalogueSync</AssemblyName>

    <!-- Recorded in the audit payload as the release identifier. Explicit
         rather than inherited, so the value in the trail is something the
         repository chose rather than a default nobody decided. -->
    <InformationalVersion>0.1.0</InformationalVersion>
  </PropertyGroup>

</Project>
