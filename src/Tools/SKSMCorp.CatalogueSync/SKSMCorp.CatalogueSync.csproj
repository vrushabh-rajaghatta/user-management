<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <InternalsVisibleTo Include="SKSMCorp.CatalogueSync.Tests" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\Platform\SKSMCorp.Platform.Persistence\SKSMCorp.Platform.Persistence.csproj" />
  </ItemGroup>

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>SKSMCorp.CatalogueSync</RootNamespace>
    <AssemblyName>SKSMCorp.CatalogueSync</AssemblyName>

    <!-- Recorded in the audit payload as the release identifier. Explicit
         rather than inherited, so the value in the trail is something the
         repository chose rather than a default nobody decided. -->
    <InformationalVersion>0.1.0</InformationalVersion>
  </PropertyGroup>

</Project>
