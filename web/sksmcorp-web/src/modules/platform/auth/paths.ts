/**
 * This module's own routes, as constants, so that code inside it can link to
 * them without importing the route table. Paths chosen by the client: nothing
 * outside the application builds links to either of them.
 */
export const SIGN_IN_PATH = "/sign-in";
