# Exact versions, so the committed lockfile and package.json never disagree
# about what "the same dependencies" means.
save-exact=true

# Refuse to install on a Node version outside "engines". jsdom and Vitest set
# the floor, and a silent install on an older Node fails later and less clearly.
engine-strict=true
